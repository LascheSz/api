let mysql;_c94‍.x([["db",()=>db]]);_c94‍.w("mysql2",[["default",["mysql"],function(v){mysql=v}]]);

const connection = mysql.createConnection({
    host: 'localhost',
    port: '3306',
    user: 'root',
    password: '',
    database: 'updates',
});

connection.connect(err => {
    if (err) {
        _c94‍.g.console.error('Error connecting to database:', err);
        return;
    }
    console.log('Connected to database successfully');
});

       const db = connection;
