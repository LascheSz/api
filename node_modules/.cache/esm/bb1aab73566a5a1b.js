let db;_566‍.w("../database/db.js",[["db",["db"],function(v){db=v}]]);

class Updates {
    static async getUpdates() {
        try {
            const [rows, fields] = await db.promise().query('SELECT * FROM updates;');
            return rows;
        } catch (error) {
            _566‍.g.console.error(error);
            return null;
        }
    }
}

module.exports = Updates;
